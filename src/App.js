import React, { Component } from "react";
import { Button } from "react-qui";

class App extends Component {
  render() {
    console.log(Button);
    return (
      <div>
        <Button>Button</Button>
      </div>
    );
  }
}

export default App;
